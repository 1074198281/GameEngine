

float CalculateOpticalLength(float height, float u, float d)
{
    int sampleCount = 500;
    float step = d / sampleCount;

    float length = 0;
    for(int i= 0; i < sampleCount; i++)
    {
        
    }
}